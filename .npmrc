# Mirror of the workspace-root .npmrc. Vercel deploying this sub-project
# directly (rather than the monorepo root) will still pick up the flag.
# See ../.npmrc for why it's needed.
legacy-peer-deps=true
